# access the vgg16 model from vgg16.vgg_models, then predict off the test images.
# construct the confusion matrix, then save to vgg16.plots

# use load_model() on a trained model, to verify that it is working properly
