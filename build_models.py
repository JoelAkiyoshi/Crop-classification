# construct and train the full-size CNN. Then use the resulting model to form the basis for the rf & svm pipelines.
# three functions in total, each returning(?) and saving a model. The pipeline runners will NOT call the first function.
